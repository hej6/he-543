'use client';
import React from 'react';

const Header: React.FC = () => {
    return (
        <header style={{ padding: '1rem', backgroundColor: '#ff00ff' }}>
            <h1>My Application Header</h1>
        </header>
    );
};
export default Header;